# PostgreSQL — module thống kê tướng (lib/stats). Bảng nằm trong schema `lol`, tự tạo khi chạy lần đầu.
DB_HOST=
DB_PORT=5432
DB_USERNAME=
DB_PASSWORD=
DB_NAME=postgres
# Tuỳ chọn: tắt SSL nếu Postgres local (mặc định bật)
# DB_SSL=false
